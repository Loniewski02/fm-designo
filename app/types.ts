type MainRoutes = {
  id: string;
  name: string;
  url: string;
};

type DesignRoutes = {
  id: string;
  name: string;
  url: string;
};

type DesignProject = {
  url: string;
  title: string;
  desc: string;
  img: string;
};
